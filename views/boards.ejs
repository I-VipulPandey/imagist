<%- include('./partials/header'); -%>
 <% if(files){ %>

<div class="jaruri">
    <div class="upper">
      <p>Board</p>
    <h2><%=board.name%></h2>

    <div class="teen-box">
      <div class="box">
        <i class="ri-star-smile-fill"></i> <span>More Ideas</span>
      </div>
      <div class="box">
        <i class="ri-git-repository-line"></i> <span>Organize</span>
      </div>
      <div class="box"><i class="ri-todo-fill"></i> <span>To-Do's</span></div>
      <div class="box"> <a href="/delete/board/<%=board._id%>"><i class="ri-delete-bin-2-fill"></i> <span>Delete Board</span></a></div>

    </div>

    <p class="pin-info"><%=board.images.length %> Pin/s in this board</p>
  <div class=" container boardImages">

    <% files.forEach(function(file,idx) { %>

    <% if(board.files.indexOf(file._id)!==-1) { %>

    <div class="window">
      <a href="/pin/<%=file._id %>">
        <img class="foto" src="/image/<%=file.filename %>" />
      </a>

      <div class="hover-zone">
        <div class="top-bar">

          <a style="background-color: #5f5e5e; background: #5f5e5e;" href="/remove/<%=file._id%>/board/<%=board.id%>">Remove </a>

          <% if (user.saved.indexOf(file._id)!==-1) { %>
          <a  type="submit"
            id="save" class="save-btn"
            style="pointer-events: all; background-color: #5f5e5e; background: #5f5e5e"
            href="/saves/pin/<%=file._id %>">Saved</a>
  
          <% } else{ %>
            
          <a class="save-btn" style="pointer-events:all;" id="save" href="/saves/pin/<%=file._id %>">Save</a>
        </form>
          <% } %>
        </div>
        <!-------------------->
        <div class="bottom-bar">
          
          <% if(image[idx].link){ %>
            <div class="gayab">
              <p>↗ <%=image[idx]?.link%></p>
            </div>
    
            <% } else { %>
            <div class="gayab"></div>
            <% } %>

          <div class="radius-ico">
            <a style="pointer-events: all;" title="Download image" href="/image/<%=file.filename%>" download="">
          
              <img
                src="https://cdn4.iconfinder.com/data/icons/navigation-40/24/download-128.png"
              />
            </a>
            <img
              src="https://cdn4.iconfinder.com/data/icons/ionicons/512/icon-more-256.png"
            />
          </div>
        </div>

        

    </div>

</div>
<% } else {  %>

<% } %>
     <% })  %>

</div>



</div>
</div>

<% } else { %>
<p>No files to show</p>
<% } %> <%- include('./partials/footer'); -%>
